# 易程智策当前技术与工作流

## 已接入能力

- **大模型**：阿里云百炼 OpenAI 兼容接口，主模型为 `qwen3.8-max`，并配置多模型切换列表。密钥只保存在运行环境，不进入代码仓库。
- **地图与天气**：高德地图负责地点解析、路线与通勤时间，高德天气负责实时天气和户外风险判断。
- **杭电助手**：按用户授权同步多学期课表、校历与教学周、考试、图书馆预约与到馆记录、二课活动、志愿活动、阳光长跑等校园数据。同步数据优先于对话生成结果，取消或变更后会在再次同步时更新。
- **数据与记忆**：SQLite 保存账号、对话与分支、用户偏好、当前计划和同步快照。不同对话共享同一用户的课表、校园数据、当前安排和长期偏好。

## 核心工作流

1. 用户登录后同步杭电助手数据，形成不可随意移动的课程、考试和预约等硬约束。
2. 规划时组合当前请求、近期对话、当前完整计划、用户偏好和杭电助手数据，交给千问做结构化理解或输出计划改动。
3. 程序合并模型结果，并核对日期、时长、先后关系、地点、开放时间、天气、通勤和吃饭留白。
4. 排程器优先满足用户本轮明确要求；长时间自习可拆成多个不少于 1 小时的时段，显式要求的总时长不得静默缩短。
5. 安排不下时返回已安排部分、冲突原因和可选调整方案，不用虚假的全绿检查掩盖漏排任务。
6. 用户确认后保存为当前计划；后续修改采用“只改指定事项，其余保持”的增量方式。

## 约束优先级

1. 用户本轮明确要求和时间边界。
2. 杭电助手同步的课程、考试、预约等事实数据。
3. 已确认的当前计划与任务依赖。
4. 用户长期偏好，如日程松紧、活动地点和常用时间。
5. 通勤更短、等待更少等优化目标。

系统正常工作依赖网络与已配置的 API。上游服务异常时应明确提示，不能用语义不可靠的本地猜测替代真实结果。

## 代码与部署

- `main` 是公网部署分支。
- 本地修改验证完成后，由维护者自行提交并推送 GitHub。
- 公网 ECS 从 GitHub `main` 拉取后执行部署脚本，Cloudflare 负责域名接入。
