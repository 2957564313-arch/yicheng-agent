# 易程智策当前技术与工作流

## 技术栈总览

| 层 | 选型 |
| --- | --- |
| 运行时 | Python 3.12，uv 管理依赖与虚拟环境 |
| 服务端 | FastAPI + Uvicorn，12 个 API 路由模块 |
| 智能体编排 | LangGraph 1.x 状态机，SQLite 检查点持久化 |
| 数据契约 | Pydantic v2（模型 JSON Schema 直接喂给大模型），pydantic-settings 读配置 |
| 大模型 | OpenAI 兼容接口（阿里云百炼），主模型 + 7 个降级模型链 |
| 外部服务 | 高德地图（地点/路线/天气）、杭电助手开放接口 |
| 存储 | SQLite ×2（业务库 + 检查点库）+ 静态 JSON + Markdown 知识库 |
| 前端 | 原生 HTML/CSS/JS，静态托管 + 严格 CSP |
| 部署 | 阿里云 ECS + Cloudflare |

## 智能体框架

核心是一个 LangGraph 状态机，**大模型只负责语言，不负责算术**。

```
understand ──► enrich ──► plan ──► validate ──► respond
     │            │                               ▲
     │            └── 纯查询意图 ─────────────────┤
     └── 缺少必要信息且无历史计划 ────────────────┘
```

- 五个节点：`理解 → 补全事实 → 排程 → 校验 → 生成回复`。
- `CampusAgentState` 是节点间唯一契约；所有仓储、Provider、服务在启动时由容器统一装配一次。
- `validate` **不回环到** `plan`：排程器是确定性的，同样的输入重跑只会得到同样的结果，因此校验失败时直接把具体冲突返回给用户，而不是空转重试。
- 每轮对话状态写入检查点库，支持对话分支与断点续接。

## 理解层：规则解析与大模型并行

这是全系统最关键的一个设计，**两者是并列关系，不是主备关系**：

- 规则解析器（约 3300 行中文模式匹配）与大模型**同时**解析同一句话，再合并。
- **硬性排程事实以规则为准**：日期、固定/可移动、明确的时钟锚点、拆段时长。
- **语言表达以模型为准**：标题、措辞、以及规则没能识别的部分。
- 两边的任务按"类型/标题/地点"相似度配对，不依赖 id。

理由：日期和时长算错就是事故，这类事实必须可预测、可测试；而自然语言的多样性交给模型。所以**理解出错通常要改规则解析器，而不是改提示词**。

修改已有计划走另一条路：模型只返回**改动本身**（增量契约），由程序应用到当前计划上。模型不允许重新输出一整天——让它重发全天时，它会丢掉用户没提到的安排。指代不明时只提问，不改动。

## 排程内核：确定性搜索

排程完全由程序完成，不经过大模型：

- 以 5 分钟为粒度为每个任务枚举候选时段，用**定宽集束搜索**（宽度 48）整体求解。
- **新建计划和改计划走同一套搜索**。逐个任务按优先级摆放会让先后顺序决定结果——灵活任务会占掉窄任务唯一能用的位置。
- 打分按**字典序**比较，优先级从高到低：
  1. 任务覆盖（有没有排不下的）
  2. 天气冲突（刻意排在扰动之上，否则改计划时"淋雨"会比"挪动"更便宜）
  3. 移动的任务数，其次是总位移量
  4. 综合成本：通勤、偏好、拖延、时长缩水
- **软性理由绝不允许删任务**：未知路线、推测出的时段偏好、下雨，都只计入代价，不作为硬性否决。
- 集束搜索是短视的——整段放得下的方案总是当场得分更高，于是"留出空间给最后一个任务"的状态会被提前剪掉。因此排不下时会按压缩档位重解，宁可把任务缩短，也不让它凭空消失。
- 长时间自习拆成多段，段间保持最小间隔，用依赖关系串起来。
- 任务模型携带的是**需求**而不只是长度：最短可压缩到多少、次数、最小间隔，以及"这个时段是用户明说的还是推测的"——用户明说的时段是硬窗口，推测的只是偏好。

## 检索

校园知识检索是**纯词法**实现：中文单字/双字/三字切分 + IDF + 一组人工调优的重排规则。知识库为 Markdown（官方文件、策展规则、导入资料）。

评测集固定 36 条标注用例，任何检索改动都以召回指标为准再决定是否合入。

## 已接入能力

- **大模型**：OpenAI 兼容接口，主模型 `qwen3.8-max`，另配 7 个降级模型依次兜底。
- **地图与天气**：高德负责地点解析、步行路线与通勤时间，以及实时天气和户外风险判断。
- **杭电助手**：按用户授权同步多学期课表、校历与教学周、考试、图书馆预约与到馆记录、二课活动、志愿活动、阳光长跑。同步数据优先于对话生成结果；取消或变更后在下次同步时更新。
- **数据与记忆**：SQLite 保存账号、对话与分支、用户偏好、当前计划和同步快照。

## 数据分层

- **静态校园事实**（JSON）：地点、通勤时间、开放时间、上课节次、校历、校园规则、天气兜底数据。
- **知识库**（Markdown）：学生手册、节假日安排、校园时间规则等。
- **业务库**（SQLite）：账号、对话、计划、课表、偏好、外部同步快照、加密后的杭电助手凭据。
- **检查点库**（SQLite）：LangGraph 运行状态。
- 浏览器另存一份个人数据副本，在请求日程时回灌，避免换设备后凉启动。

## 核心工作流

1. 用户登录后同步杭电助手数据，形成不可随意移动的课程、考试和预约等硬约束。
2. 规划时组合当前请求、近期对话、当前完整计划、用户偏好和杭电助手数据，交给大模型做结构化理解或输出计划改动。
3. 程序合并模型结果，并核对日期、时长、先后关系、地点、开放时间、天气、通勤和吃饭留白。
4. 排程器优先满足用户本轮明确要求；长时间自习可拆成多个不少于 1 小时的时段，显式要求的总时长不得静默缩短。
5. 安排不下时返回已安排部分、冲突原因和可选调整方案，不用虚假的全绿检查掩盖漏排任务。
6. 用户确认后保存为当前计划；后续修改采用"只改指定事项，其余保持"的增量方式。

## 约束优先级

1. 用户本轮明确要求和时间边界。
2. 杭电助手同步的课程、考试、预约等事实数据。
3. 已确认的当前计划与任务依赖。
4. 用户长期偏好，如日程松紧、活动地点和常用时间。
5. 通勤更短、等待更少等优化目标。

## 降级与离线

每个外部依赖都有确定性兜底，**上游挂掉不会让整个规划失效**：

- 路线：实时高德失败时退回本地缓存坐标做保守估算，并在结果上标注"已降级"。
- 天气：实时接口只覆盖近期窗口；历史演示日期使用固定数据，并明确标注为演示数据，不冒充实时结果。
- 大模型：主模型失败时沿降级链依次重试。
- 提供离线模式，测试与演示可完全不联网运行。

降级一律显式标注来源，绝不用不可靠的本地猜测冒充真实结果。

## 前端与接口

- 单页原生实现，无打包工具、无前端框架；静态文件由服务端直接托管。
- 严格 CSP（禁止外部脚本/样式/字体，禁止被嵌套），并统一下发安全响应头。
- 页面入口文件禁用缓存；**静态资源靠 URL 版本号更新**（改图标或样式后必须同步改版本号，否则老用户会继续看到旧文件）。
- 错误统一走应用错误类型，回包带请求 ID 与追踪 ID。
- API 文档默认关闭，仅在本地或维护窗口开启。

## 安全

- 接口默认需要 Bearer 令牌，仅健康检查、登录和少量只读接口公开。
- 个人空间按用户 id 隔离，越权访问直接拒绝。
- 杭电助手凭据加密存储，密钥只来自运行环境。

## 质量保障

- 45 个测试文件，分三层：单元、集成、**验收**（按"学生眼里正确的一天"来写，而不是按实现来写）。
- 24 个冻结的排程基准场景：任何改动打分权重的修改都要跑基准前后对比。
- 36 条检索标注用例，配套召回评分脚本。
- 静态数据完整性校验脚本；ruff 静态检查。

## 代码与部署

- `main` 是公网部署分支。
- 本地修改验证完成后，由维护者自行提交并推送 GitHub。
- 公网 ECS 从 GitHub `main` 拉取后执行部署脚本，Cloudflare 负责域名接入。
