# 易程智策项目交付清单与待办

> 更新时间：2026-07-24  
> 当前状态：本地完整应用已完成；千问、高德路线和高德天气已真实联调；
> 当前开发范围冻结在 localhost，不继续公网部署。

---

## 一、当前可运行成果

项目目录：

```text
/Users/xuwenhang/Desktop/易程智策项目
```

当前技术组成：

| 部分 | 当前实现 |
|---|---|
| 智能体流程 | LangGraph 单状态图 |
| 后端与网页入口 | FastAPI + 单页 Console |
| 需求理解模型 | qwen3.6-flash |
| 模型策略 | 关闭深度思考；10 秒超时；失败自动切换本地解析 |
| 精确规划 | Python 排程、冲突检查和局部重规划 |
| 地图与路线 | 高德 Web 服务步行路线 |
| 天气 | 高德天气预报 |
| 校园知识 | 学生手册 + 已核验校园服务时间知识 |
| 知识检索 | 本地增强检索：查询扩展、来源分级、重排去重 |
| 数据保存 | SQLite（计划、消息、运行记录和长期记忆） |
| 离线保障 | 本地地点、路线快照、天气 fixture 和规则解析 |

系统流程：

```text
用户输入
  → 需求理解
  → 校园知识、地点、路线和天气补全
  → 日程规划
  → 硬约束校验
  → 必要时重新规划
  → 输出并保存当前有效计划
```

### 已实现功能

- 口语化任务解析；
- 日期、时间、地点、时长和截止时间提取；
- “第 N 节有课”等校内作息换算并锁定为不可移动硬约束；
- 校园地点名称与别名标准化；
- 实时步行距离和时间查询；
- 实时天气查询；
- 任务优先级和先后依赖；
- 无冲突日程生成；
- 通勤、截止时间、开放时间和天气风险校验；
- 当前计划与历史计划保存；
- 需求变化后的局部重规划；
- 新旧计划差异展示；
- 外部模型、地图或天气异常时自动降级；
- 三个可重复的一键演示案例；
- 校园制度和学生手册问答。
- 跨会话长期偏好记忆及前端手动增改、启停和删除；
- 已核验知识、天气、拥堵时段和个人偏好的综合提醒；
- 默认隐藏内部标识与英文变量，开发信息集中到折叠调试区。

---

## 二、当前模型配置

正式主模型：

```text
qwen3.6-flash
```

关键策略：

```dotenv
LLM_ENABLED=true
LLM_MODEL=qwen3.6-flash
LLM_ENABLE_THINKING=false
LLM_RENDER_ENABLED=true
LLM_TIMEOUT_SECONDS=10
```

设计说明：

- 千问负责自然语言需求理解；
- 明确的开始时间、截止时间和任务顺序由本地规则交叉校验；
- 模型输出违反显式约束时，以确定性约束为准；
- 程序先生成严格依据结构化结果的回复草稿，再由千问进行语气润色；
- 模型润色不得修改时间、任务、缺口或建议，校验不通过时自动使用草稿；
- 模型超时、接口异常或 JSON 不合法时，系统继续使用本地解析器；
- API Key 只保存在本地 `.env`，不会写入代码、文档或页面。

真实复杂输入验证结果：

```text
输入：
今天14点后去图书馆自习2小时，再去菜鸟驿站取快递，
最后去东操场跑步30分钟，18点前结束。

结果：
14:00—16:00 图书馆自习
16:15—16:45 取快递
17:00—17:30 跑步

状态：
模型理解成功
实时路线成功
实时天气成功
硬约束违反 0
响应时间 7.17 秒
告警 0
```

---

## 三、高德地图与天气

### 1. 已完成的实时能力

- Web 服务 Key 已配置；
- 关键字地点搜索已验证；
- 步行路径规划已验证；
- 天气预报已验证；
- 钱塘区天气编码 `330114` 已写入校园配置；
- 路线请求加入每秒 3 次以内的限速；
- QPS 超限时自动等待并重试；
- 同一进程内重复路线和同一天气使用缓存；
- API 超时、额度用尽或地点含糊时自动使用本地数据。

### 2. 已定位地点

| 系统地点 | 高德结果 |
|---|---|
| 第六教学楼 | 第六教学科研楼入口 |
| 图书馆 | 下沙校区图书馆入口 |
| 菜鸟驿站 | 学林街店入口 |
| 学校食堂 | 默认使用第三餐厅入口 |
| 东操场 | 下沙校区田径场入口 |
| 实验室 | 名称不唯一，继续要求用户指定具体楼宇或使用本地估算 |

### 3. 已固化的本地路线快照

七组核心路线已由高德真实查询并保存到
`data/travel_times.json`。例如：

| 路线 | 距离 | 步行时间 |
|---|---:|---:|
| 六教 → 图书馆 | 297 米 | 4 分钟 |
| 图书馆 → 菜鸟驿站 | 903 米 | 13 分钟 |
| 图书馆 → 第三餐厅 | 336 米 | 5 分钟 |
| 菜鸟驿站 → 第三餐厅 | 562 米 | 8 分钟 |
| 第三餐厅 → 田径场 | 479 米 | 7 分钟 |
| 图书馆 → 田径场 | 699 米 | 10 分钟 |
| 菜鸟驿站 → 田径场 | 1071 米 | 15 分钟 |

因此，比赛现场即使高德接口不可用，核心校园路线仍能规划。

### 4. 费用与认证

高德页面显示的“30 元/万次”是超出免费月配额后的流量包价格。
当前还需在高德控制台确认账号已完成“个人开发者认证”。个人认证账户的
月免费配额足以覆盖比赛测试，但应在控制台继续开启额度提醒并避免购买
不必要的流量包。

---

## 四、知识库

当前知识库由两部分组成：

### 1. 正式学生手册

```text
data/knowledge/official/2025年学生手册.md
```

- 来源：用户提供的《2025年学生手册（终稿）》PDF；
- PDF 共 206 页；
- 已完整转为本地可检索 Markdown；
- 保留源文件哈希和来源信息；
- 已标记为项目方核验内容。

### 2. 校园服务时间与规则

包括：

- 13 节课的上下课时间；
- 阳光长跑时段和场地；
- 图书馆各楼层开放时间；
- 快递点营业时间；
- 宿舍门禁与熄灯时间；
- 体育场馆开放规则；
- 餐厅服务时段；
- 热水开放时间；
- 校医院就诊时间；
- 校园拥堵时段。

用户已确认这部分内容经过核验。

### 3. RAG 当前状态

- 当前索引约 97 个知识分块；
- 已启用查询扩展、精确短语加权、正式/整理/导入来源分级、
  二阶段重排与相似段落去重；
- 正式制度文件优先于普通整理文本；
- 结构化开放时间优先于 RAG 文本；
- RAG 不负责路线计算、天气、冲突检查和日程求解；
- 制度问答没有足够证据时不会编造答案。

### 4. 切换其他学校

程序逻辑与学校数据已分离。其他高校不需要修改 LangGraph 或 FastAPI，
只需更换一套 Campus Profile：

```text
campus_profile.json
locations.json
travel_times.json
opening_hours.json
campus_rules.json
class_periods.json
knowledge/
```

只替换学生手册即可迁移制度问答；要迁移完整时空规划，还需提供本校地点、
开放时间、节次和路线数据。当前版本一次部署加载一所学校配置。

---

## 五、三个固定演示

### 案例一：常规规划

```text
14:00—16:00 图书馆学习
16:00—16:13 前往菜鸟驿站
16:15—16:45 取快递
16:45—17:00 前往东操场
17:00—17:30 跑步
```

### 案例二：需求变化

把学习延长 30 分钟：

```text
14:00—16:30 图书馆学习
16:30—16:43 前往菜鸟驿站
16:45—17:15 取快递
17:15—17:30 前往东操场
17:30—18:00 跑步
```

学习延长 30 分钟，后两项只顺延 30 分钟，仍在 18:00 前完成。

### 案例三：天气调整

固定演示天气为 17:00 后降雨：

```text
14:00—14:30 跑步
14:30—14:40 前往图书馆
14:40—16:40 图书馆学习
16:40—16:53 前往菜鸟驿站
16:55—17:25 取快递
```

天气会改变实际日程，不只是追加提醒。

---

## 六、测试与验收

当前结果：

```text
自动化测试：60 项全部通过
离线评估：60/60 通过
三个固定 Demo：全部有效，硬约束违反均为 0
真实模型＋路线＋天气端到端：通过
端到端响应时间：7.17 秒
```

主要脚本：

| 脚本 | 用途 |
|---|---|
| `scripts/validate_static_data.py` | 校园配置完整性校验 |
| `scripts/run_evaluation.py` | 60 组离线评估 |
| `scripts/demo_smoke.py` | 三个固定 Demo 回归 |
| `scripts/live_llm_smoke.py` | 千问与知识问答检查 |
| `scripts/live_amap_smoke.py` | 高德路线和天气检查 |
| `scripts/live_end_to_end_smoke.py` | 模型、路线、天气联合验收 |
| `scripts/probe_qwen_model.py` | 千问模型兼容性和速度检测 |

报告：

```text
reports/evaluation.md
reports/evaluation.json
reports/browser_acceptance.md
reports/release_checklist.md
```

---

## 七、本地启动

最简单的方式是双击：

```text
/Users/xuwenhang/Desktop/易程智策项目/启动易程智策.command
```

或在终端执行：

```bash
cd /Users/xuwenhang/Desktop/易程智策项目
.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000
```

然后打开：

```text
http://127.0.0.1:8000/
```

本地地址只用于开发和验收，不能填入参赛材料。

---

## 八、当前范围与后续可选项

当前交付范围已经完成并冻结在：

```text
http://127.0.0.1:8000/
```

公网部署、HTTPS 地址、入口二维码和生产环境复测均暂不执行。若未来恢复
公网发布，再按照 `docs/deployment.md` 和 `reports/release_checklist.md`
继续，不需要重构本地应用。

### 功能冻结

不再新增以下功能：

- 多智能体协商；
- 强化学习；
- 独立小程序；
- 实时食堂人流；
- 图书馆座位；
- 后台主动推送；
- 多学校同时在线切换；
- 复杂权限系统。

这些内容只放入未来规划。

---

## 九、安全规则

- `.env` 已加入 `.gitignore`；
- 任何 Key 都不得出现在 Markdown、PPT、截图、Git 或前端 JavaScript；
- 公网部署后 Key 只放服务器环境变量；
- 高德 Key 可在获得固定服务器出口 IP 后配置 IP 白名单；
- 不在浏览器前端直接调用高德 Web 服务；
- 不向评委提供管理员账号；
- 正式入口默认无需登录即可体验；
- 若平台强制登录，只创建独立测试账号。

---

## 十、最终判断

当前代码、知识库、大模型、实时路线、实时天气、离线降级、固定 Demo、
测试和本地网页均已完成。工程不需要再次重搭，当前不继续公网发布。
