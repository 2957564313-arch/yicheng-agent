# 知识库导入状态

当前工作区尚未挂载 `coze.zip`，因此这里没有填充校园事实。

附件可访问后，导入程序应：

1. 只读解压到临时目录；
2. 识别 PDF、DOCX、TXT、Markdown、CSV 和 JSON；
3. 保留原始文件名、来源和更新时间；
4. 将结构化地点、开放时间和路线优先写入对应 JSON；
5. 将剩余说明性内容转换为 Markdown；
6. 运行数据校验和检索测试；
7. 不把 Prompt、平台变量或密钥写入知识库。

